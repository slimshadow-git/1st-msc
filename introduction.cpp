#include <iostream>  // This is a header file that includes the standard input/output stream objects

int main() {
    std::cout << "Hello, World!" << std::endl;  // This line prints "Hello, World!" to the console
    return 0;  // The program ends here, returning 0 indicates successful execution
}
